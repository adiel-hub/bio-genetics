services:
  - type: web
    name: biogenetics-relay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT relay_server:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
